# Server
PORT=3001
NODE_ENV=development

# API base URL for frontend (set in production)
# FRONTEND_ORIGIN=https://your-app.vercel.app

# Database path (default: data/expenses.db relative to backend root)
# DB_PATH=./data/expenses.db
